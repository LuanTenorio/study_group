export interface CreateMaterial {
  title: string;
  description: string;
  group_id: number;
  user_id: number;
}

export interface UpdateMaterial {
  title: string;
  description: string;
  group_id: number;
}
